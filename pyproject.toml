[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yokogawa-cv8000-compiler"
version = "0.1.0"
description = "Process output data from Yokogawa Cell Voyager CV8000"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = [
    "numpy",
    "pandas",
    "tifffile",
    "xmltodict"
]

[project.scripts]
compile-cv8000 = "yokogawa_cv8000_compiler.__main__:main"
compress-tiffs = "yokogawa_cv8000_compiler.tiff_compression:main"

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["."]

[tool.black]
line-length = 88

[tool.isort]
profile = "black"